server {
    listen 443 ssl;
    server_name _;
    ssl_certificate /etc/ssl/goldenmean.pro/certificate.crt;
    ssl_certificate_key /etc/ssl/goldenmean.pro/private.key;

    client_max_body_size 15M;

    root /var/www/hackathon-frontend-build;
    index index.html;

    gzip  on;

    location / {
        try_files $uri $uri/ /index.html;
        gzip_static on;
        gzip_types text/plain text/css application/json application/x-javascript text/xml application/xml application/xml+rss text/javascript application/javascript;
    }
    location /api {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Real-IP $remote_addr;
        }
    location /admin {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Real-IP $remote_addr;
        }
    location /media {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Real-IP $remote_addr;
    }
    location /static {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
server {
    listen 80;
    server_name _;
    return 301 https://goldenmean.pro$request_uri;
}
# ln -s /var/www/hackathon/backend/goldenmean.pro.nginx.conf /etc/nginx/sites-enabled/goldenmean.pro.nginx.conf
# ln -s /var/www/hackathon/backend/goldenmean.pro.nginx.conf /etc/nginx/sites-available/goldenmean.pro.nginx.conf
# scp ./hackathon-frontend-build.zip root@94.142.142.59/var/www/
